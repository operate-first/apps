---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Add a cluster set here if you want it bound to argocd/idp-auth-primary/etc. namespaces
resources:
  - argocd-managed.yaml
  - octo-ushift-dev.yaml
  - osc.yaml
