kind: ResourceQuota
apiVersion: v1
metadata:
  name: thoth-frontend-prod-custom
spec:
  hard:
    requests.cpu: '8'
    requests.memory: 12Gi
    limits.cpu: '8'
    limits.memory: 12Gi
    requests.storage: 40Gi
