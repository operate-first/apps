apiVersion: v1
kind: LimitRange
metadata:
  name: default
spec:
  limits:
    - type: Container
      default:
        cpu: "2"
        memory: 8Gi
      defaultRequest:
        cpu: 300m
        memory: 400Mi
