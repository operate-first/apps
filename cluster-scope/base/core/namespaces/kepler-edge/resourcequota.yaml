apiVersion: v1
kind: ResourceQuota
metadata:
  name: kepler-edge-custom
spec:
  hard:
    limits.cpu: '20'
    limits.memory: 50Gi
    requests.cpu: '12'
    requests.memory: 50Gi
    requests.storage: 60Gi
