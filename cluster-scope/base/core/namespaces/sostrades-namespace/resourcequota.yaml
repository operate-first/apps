apiVersion: v1
kind: ResourceQuota
metadata:
  name: sostrades-namespace-custom
spec:
  hard:
    limits.cpu: '40'
    limits.memory: 80Gi
    requests.cpu: '6'
    requests.memory: 10Gi
    requests.storage: 20Gi
    count/objectbucketclaims.objectbucket.io: 1
