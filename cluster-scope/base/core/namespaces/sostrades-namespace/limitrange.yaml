kind: LimitRange
apiVersion: v1
metadata:
  name: sostrades-limitrange-custom
spec:
  limits:
    - type: Container
      default:
        cpu: '1'
        memory: 1Gi
      defaultRequest:
        cpu: 300m
        memory: 400Mi
