apiVersion: v1
kind: ResourceQuota
metadata:
  name: opf-kafka-custom
spec:
  hard:
    limits.cpu: '16'
    limits.memory: 200Gi
    requests.cpu: '8'
    requests.memory: 100Gi
    requests.storage: 700Gi
