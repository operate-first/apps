apiVersion: v1
kind: ResourceQuota
metadata:
  name: thoth-frontend-prod-custom
spec:
  hard:
    limits.cpu: '8'
    limits.memory: 12Gi
    requests.cpu: '8'
    requests.memory: 12Gi
    requests.storage: 40Gi
