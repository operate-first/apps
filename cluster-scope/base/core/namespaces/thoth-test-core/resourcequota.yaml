apiVersion: v1
kind: ResourceQuota
metadata:
  name: thoth-test-core-custom
spec:
  hard:
    limits.cpu: '8'
    limits.memory: 32Gi
    requests.cpu: '8'
    requests.memory: 32Gi
    requests.storage: 40Gi
