apiVersion: v1
kind: ResourceQuota
metadata:
  name: opf-pulp-custom
spec:
  hard:
    requests.cpu: "8"
    requests.memory: 16Gi
    limits.cpu: "8"
    limits.memory: 16Gi
    requests.storage: 96Gi
