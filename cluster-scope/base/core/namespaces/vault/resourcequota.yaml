kind: ResourceQuota
apiVersion: v1
metadata:
  name: vault-custom
spec:
  hard:
    requests.cpu: '500m'
    requests.memory: 2Gi
    limits.cpu: '500m'
    limits.memory: 2Gi
    requests.storage: 60Gi
    count/objectbucketclaims.objectbucket.io: 1
