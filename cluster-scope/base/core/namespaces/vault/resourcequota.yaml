kind: ResourceQuota
apiVersion: v1
metadata:
  name: vault-custom
spec:
  hard:
    requests.cpu: '1'
    requests.memory: 2Gi
    limits.cpu: '1'
    limits.memory: 2Gi
    requests.storage: 60Gi
    count/objectbucketclaims.objectbucket.io: 1
