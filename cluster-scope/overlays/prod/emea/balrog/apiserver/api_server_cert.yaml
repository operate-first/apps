apiVersion: config.openshift.io/v1
kind: APIServer
metadata:
  name: cluster
spec:
  servingCerts:
    namedCertificates:
      - names:
          - api.balrog.aws.operate-first.cloud
        servingCertificate:
          name: api-certificate
