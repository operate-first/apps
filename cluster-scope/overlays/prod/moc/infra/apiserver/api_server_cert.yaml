apiVersion: config.openshift.io/v1
kind: APIServer
metadata:
  name: cluster
spec:
  servingCerts:
    namedCertificates:
      - names:
          - api.moc-infra.massopen.cloud
        servingCertificate:
          name: api-certificate
