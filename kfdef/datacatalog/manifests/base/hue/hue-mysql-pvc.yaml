kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: hue-mysql
  annotations:
    volume.beta.kubernetes.io/storage-class: "$(storage_class)"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "1Gi"
