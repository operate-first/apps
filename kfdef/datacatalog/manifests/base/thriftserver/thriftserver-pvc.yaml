---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: thriftserver
  annotations:
    volume.beta.kubernetes.io/storage-class: "$(storage_class)"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "5Gi"
