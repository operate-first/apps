apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
    name: scanner-tls
spec:
    dataFrom:
        - extract:
              key: moc/smaug/stackrox/scanner-tls
    secretStoreRef:
        kind: SecretStore
        name: opf-vault-store
    target:
        name: scanner-tls
        template:
            engineVersion: v2
