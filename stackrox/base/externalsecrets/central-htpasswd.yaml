apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
    name: central-htpasswd
spec:
    dataFrom:
        - extract:
              key: moc/smaug/stackrox/central-htpasswd
    secretStoreRef:
        kind: SecretStore
        name: opf-vault-store
    target:
        name: central-htpasswd
        template:
            engineVersion: v2
