apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
    name: scanner-db-password
spec:
    dataFrom:
        - extract:
              key: moc/smaug/stackrox/scanner-db-password
    secretStoreRef:
        kind: SecretStore
        name: opf-vault-store
    target:
        name: scanner-db-password
        template:
            engineVersion: v2
