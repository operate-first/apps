apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: domains
  description: A collection of all operate-first domains
spec:
  targets:
    - ./domains/community.yaml
    - ./domains/platform.yaml
    - ./domains/sre.yaml
