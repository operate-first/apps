apiVersion: v1
data:
  # Should match "secret" key in superset-custom k8s secret
  SUPERSET_SECRET: changeme
  # Should match "secret" key in trino-oauth k8s secret
  TRINO_SECRET: changeme
immutable: false
kind: Secret
metadata:
  name: dex-clients
type: Opaque
