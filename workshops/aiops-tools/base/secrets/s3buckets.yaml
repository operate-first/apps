apiVersion: v1
data:
  AIOPS_TOOLS_WORKSHOP_REGION: ""
  AIOPS_TOOLS_WORKSHOP_S3_ENDPOINT: s3endpoint.com
  AIOPS_TOOLS_WORKSHOP_S3_ENDPOINT_FULL: https://s3endpoint.com
  AIOPS_TOOLS_WORKSHOP_S3_ENDPOINT_URL_PREFIX: https://
  # If using minio, this should match the bucket name/creds created in minio
  # See minio secret for details on creds
  AIOPS_TOOLS_WORKSHOP_AWS_ACCESS_KEY_ID: VALUE
  AIOPS_TOOLS_WORKSHOP_AWS_SECRET_ACCESS_KEY: VALUE
  AIOPS_TOOLS_WORKSHOP_BUCKET: bucketname
immutable: false
kind: Secret
metadata:
  name: s3buckets
type: Opaque
