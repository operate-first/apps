extends: default
rules:
  line-length: disable
  document-start:
    present: false
  indentation:
    indent-sequences: whatever
  hyphens:
    max-spaces-after: 4
ignore: |
  scripts/setup_pvs/playbook.yaml
