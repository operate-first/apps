apiVersion: integreatly.org/v1alpha1
kind: GrafanaDashboard
metadata:
  labels:
    app: grafana
  name: cluster-pod-overview
spec:
  customFolderName: Cluster Management
  configMapRef:
    name: compute-resources-overview
    key: cluster-pod-overview.json
