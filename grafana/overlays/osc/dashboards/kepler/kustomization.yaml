---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - grafanadashboard.yaml
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - files:
      - files/kepler.json
    name: kepler
