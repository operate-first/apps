kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: trino-secure
  annotations:
    kubernetes.io/tls-acme: "true"
spec:
  host: trino.apps.devconfus2021.aws.operate-first.cloud
  to:
    kind: Service
    name: trino-service
  port:
    targetPort: 8080
